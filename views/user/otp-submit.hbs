<div class="container">
  <div class="row">
    <div class="col-md-6">
    <head>

    </head>
    <body>
      <div class="container">
        <div class="row main-content text-center">
          <div class="col-md-4 text-center company_info">
            <span class="company-logo"><h2><span
            class="fa fa-commenting-o fa-3x"
         
            <h4 class="company_title"></h4>
          </div>
          <div class="col-md-8 col-xs-12 col-sm-12 login_form">
            <div class="container">
              <div class="row">
                <h2>Enter OTP</h2>
              </div>

              <div class="row">



                <form 
                control=""
                class="form-group"
                action="/otp-submit"
                method="post"
                id="form"
                >
                {{#if errOtp}}
                <h5 style="color:crimson;">{{errOtp}}</h5>
                {{/if}}

                <div class="row">
                  <input
                  type="text"
                  name="otp"
                  id="otp-number"
                  class="form-input"
                  placeholder="Enter OTP"/>

                  <span id="errorNumber" style="color:crimson"></span>
                </div>
                
                <div class="row">
                  <button type="submit" class="btn btn-primary mt-5">Verify</button>
                </div>
                <div><span id="timer"></span> </div>
                <div class="row">
                  <p><a id="resent" href="/resend-otp"></a></p>
                </div>
                
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </body>
    </div>
  </div>
</div>


<script>
  const input=document.getElementById("otp-number")t
  const form=document.getElementById("form")
  const error1=document.getElementById("errorNumber")

  form.addEventListener(e)=>{
    e.preventDefault()

    let message=[]
    if(inputvalue.length==''||input.value==null){
      message.push("Enter phone number ")
    }else if(isNaN(input.value)==true){
      message.push('only numbers allowed')
    }
    if(message.value>0){
      e.preventDefault()
      error1.innerText=message.join('')
    }
  }
</script>